# PixiJS with TypeScript and Webpack
Seed for PixiJS project with TypeScript and Webpack.
## Setup
```
npm run start	// Install packages and serve
```
## Usage
```
npm run serve	// Run local build
npm run lint
npm run buid
```